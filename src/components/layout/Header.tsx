
import HeaderMain from "../ui/HeaderMain";

type Props = {};

const Header = ({}: Props) => {
  return (
    <div className="max-w-max mx-auto px-3 ">
      <HeaderMain />
    </div>
  );
};

export default Header;
